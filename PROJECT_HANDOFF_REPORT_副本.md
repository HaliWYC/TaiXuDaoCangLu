# 《太虚道藏录》项目交接报告

> 面向后续参与项目评估、架构调整与玩法设计的游戏开发顾问。  
> 基于 2026-07-26 当前仓库、Unity 场景、项目配置和主要运行时代码整理。

## 一、游戏当前定位和核心玩法

《太虚道藏录》目前定位为一款 2D 像素风修仙题材 RPG，核心战斗采用网格战棋形式。现阶段代码体现出的预期体验是：玩家在由多个场景组成的修仙世界中自由移动和探索，进入战斗后切换为基于行动进度的回合制网格战斗，通过角色属性、境界、功法、法术、法宝和五行道藏进行构筑与决策。

当前最有辨识度的战斗资源是“五行道藏”。角色按灵根比例在回合开始时获得锐金、灵木、弱水、离火、厚土五类道藏；法术除气血、法力、体力、精神力和冷却外，还会消耗指定或同类道藏。代码与数据结构还预留了五行相生、相克所触发的额外法术效果。

当前可识别的核心循环为：

1. 在 Forest、House 等 Tilemap 场景中移动和探索。
2. 通过传送点在 Addressables 场景之间切换。
3. 遭遇角色后进入战斗，按“反应”属性积累行动进度。
4. 在网格上移动、选择法术范围与目标，结算命中、闪避、暴击、防御减伤和效果。
5. 通过背包、装备、携带物品、功法和境界改变角色能力。
6. 随游戏时间推进获得修炼经验、年龄增长并尝试突破境界。

目前这是“系统原型”而非内容完整版本：已有玩家、一个法师怪物、一个基础火球术、一个基础功法和少量装备/储物袋数据，足以验证系统方向，但尚未形成任务、成长、探索奖励、战斗结算和长期存档组成的完整游戏闭环。

## 二、Unity 版本与主要依赖

项目当前使用：

- Unity `6000.4.8f1`，revision `f8b72d3d7343`。
- Addressables `2.9.1`：管理 Persistent、UI、Forest、House 等场景。
- Cinemachine `3.1.6`：游戏相机与场景边界约束。
- Input System `1.19.0`：玩家移动、法术快捷键和携带物品输入。
- Universal RP `17.4.0`：已安装并存在 URP 资源，但 `GraphicsSettings` 当前未指定全局 Render Pipeline Asset，实际项目仍按 Built-in 管线运行。必须尽快决定是否正式迁移 URP。
- UGUI `2.0.0` 与 TextMesh Pro：当前全部游戏界面。
- DOTween：以源码插件形式位于 `Assets/Plugins/Demigiant/DOTween`，用于移动、法术表现、淡入淡出和 UI 动画。
- Unity Test Framework `1.6.0`：包已安装，但项目没有正式 EditMode/PlayMode 测试程序集。

此外安装了 Version Control、Rider、Visual Studio、Timeline、Multiplayer Center 等包，其中部分尚未出现在核心运行时代码中，可在稳定构建后审查并精简。

当前 Build Settings 包含 Boot、Forest、House、Persistent、Template、UI 六个场景；Addressables 的 Scene 组包含 Forest、House、Persistent、UI。Boot 是启动入口，Persistent 和 UI 以附加场景加载，具体地图场景再由 TransitionManager 管理。

## 三、Assets 目录和代码架构

Assets 可按职责概括为以下层次：

- `Art`、`Animation`、`Animator`、`Font`、`Tilemaps`、`Sprite Altas`：角色、环境、法术、UI 和字体资源。包含第三方像素素材与 GUI 包。
- `GameData`：以 ScriptableObject 为中心的策划数据，包括角色、法术、功法、物品、背包和地图层级。
- `Prefabs`：玩家、NPC、法术衍生物、场景树木和 UI 预制体。
- `Scenes`：启动、常驻、UI 和当前可探索地图场景。
- `AddressableAssetsData`：场景 Addressables 配置。
- `Scripts`：按 AStar、Character、Combat、Effect、Inventory、Manager、Map、Player、Transition、UI、Utilities、XiuLian 分区。

运行时架构以 Persistent 场景中的多个 `Singleton<T>` 管理器为核心，通过静态 `EventHandler` 事件总线连接时间、场景、战斗、效果和背包 UI。地图场景以 Additive 方式装卸，玩家与各全局管理器留在 Persistent 场景。

数据层主要使用 ScriptableObject；运行时通过 `Instantiate` 克隆角色、法术、背包和物品数据，避免直接修改项目资产。地图使用 World → Map → District → SceneData 的层级模型；当前真正参与运行的主要是 SceneData，其中保存 Addressable 场景引用、网格尺寸、障碍数据和场景角色出生信息。

当前没有 `.asmdef`，项目代码、编辑器工具和第三方运行时代码大多进入默认程序集。静态事件、场景对象引用和单例依赖较多，模块边界在目录层面清楚，但在编译和运行时耦合层面仍较强。

## 四、已完成或已具备原型能力的系统

以下“已完成”指存在可运行的原型能力，不代表已达到生产质量：

- 启动与场景切换：Boot 加载 Persistent/UI；地图场景通过 Addressables 加载，并带淡入淡出、玩家位置迁移和活动场景切换。
- 2D 地图移动：Input System 驱动玩家 Rigidbody2D 移动，Cinemachine 负责相机与边界。
- 网格与寻路：SceneData 保存网格尺寸和障碍；自研 A* 支持四方向寻路；战斗网格可以显示移动范围、确认路径、施法范围和效果覆盖范围。
- 回合战斗骨架：按反应属性累计行动条，玩家和敌人进入回合；移动力、法术冷却、准备回合、道藏分配和回合 UI 已接入。
- 基础战斗结算：包含命中/闪避、暴击/化劲、防御减伤、气血扣除、受伤和死亡状态。
- 法术系统骨架：ScriptableObject 法术数据、施法条件、资源消耗、目标筛选、法术表现对象池和 EffectData 效果执行链。
- NPC 战斗 AI 原型：随机选择敌人和可用法术，必要时 A* 接近目标后施法。
- 角色属性与境界：基础、境界、装备、功法属性分层后汇总；境界数值从 CSV 文本初始化，经验达到阈值后推进小境界/大境界。
- 功法系统骨架：主功法/副功法列表、修炼速度、属性加成以及随日期推进经验。
- 背包与装备：分类背包、储物袋容量、物品堆叠、拖放换位、装备栏、携带栏、装备属性汇总和 UI。
- 时间系统：游戏时、日、月、季、年推进及事件广播；UI 和修炼逻辑已监听时间事件。
- 基础 UI：战斗行动条、角色状态、法术槽、道藏选择、携带物品、背包、Tooltip、菜单和场景淡入淡出。

## 五、尚未完成或仅预留数据结构的系统

- 正式存档/读档、自动存档、版本迁移与损坏恢复完全未实现。
- 战斗结束判定、胜负结算、死亡对象清理、奖励、脱战和恢复世界时间未形成完整流程。
- 任务、剧情、对话、商店、经济循环、掉落、交互、采集、制作等 RPG 主循环尚未实现。
- 世界地图、区域和地点类型虽然有数据模型，但缺少完整导航界面和实际玩法。
- 炼丹、炼器、制符、阵法仅存在属性和物品分类，没有生产系统。
- 法术准备/持续施法、特殊施法条件、法术升级/熟练度、进化/退化、神通和秘术规则未完整实现。
- Effect 系统只有伤害效果形成具体实现；持续、永久、Buff/Debuff 生命周期仍不完整。
- 功法学习、切换、容量限制、功法效果、进化/退化以及修炼操作界面未完成。
- 消耗品、任务物品、普通材料在战斗中的使用行为大多为空分支。
- NPC 非战斗行为、感知、阵营关系和战斗触发流程未形成通用系统。
- 设置、音频、本地化、按键重映射、辅助功能与正式主菜单流程缺失。
- 缺少目标平台的正式构建、CI、自动化测试和性能基线。

## 六、核心脚本及其职责

- `StartGame`：启动时加载 Persistent 与 UI Addressable 场景。
- `TransitionManager`：负责当前地图场景的异步卸载/加载、活动场景设置、玩家传送和淡入淡出。
- `EventHandler`：全局静态事件总线，连接时间、场景、战斗、效果和背包 UI。
- `Singleton<T>`：全局管理器的基础单例实现。
- `GameManager`：持有玩家和主相机引用，控制相机尺寸。
- `TimeManager`：推进游戏内时间并发出小时、日期、季节事件。
- `GridMapManager`：把 SceneData 的 TileProperty 转换为可寻路网格，并按场景名提供网格。
- `AStar`：执行四方向最短路径搜索并生成 MovementStep。
- `CharacterBase`：角色运行时核心，负责属性初始化/汇总、战斗数值、伤害、状态、道藏、境界和法术冷却。
- `Player`：处理玩家输入、非战斗移动、战斗注册、快捷法术与携带物品选择。
- `NPCController`：当前敌方战斗 AI，负责选敌、选法术、寻路接近和施法。
- `CharacterManager`：提供创建角色附加属性层时使用的 CharacterData 模板，并要求先于角色 Awake 初始化。
- `CharacterJingjieManager`：从 CSV 文本建立境界属性字典。
- `CombatManager`：维护参战角色、阵营、行动进度和当前回合。
- `CombatGridManager`：维护战斗角色网格位置，计算和显示移动/施法范围，并确认移动与施法目标。
- `CombatMovement`：将网格路径转换为 DOTween 移动。
- `FaShuManager`：注册法术数据，检查施法条件，发起表现、执行效果并扣除资源。
- `FaShuDerivative` / `PoolTool`：管理法术视觉对象及其对象池，在动画阶段触发效果结算。
- `EffectData` / `DamageEffect`：效果抽象及当前唯一明确实现的伤害效果。
- `GongFaProcessor`：汇总功法属性并按游戏日期增长修炼经验。
- `InventoryManager`：物品查询、换位、堆叠、容量、装备、卸装、增删物品等背包业务。
- `InventoryUI` / `CombatUI`：分别负责背包交互和战斗 HUD/携带物品/行动条。

## 七、数据存储和存档方案

当前静态配置数据主要保存在 ScriptableObject：

- `CharacterData`：角色基础信息、修炼属性、战斗属性、五行灵根/道藏、精神力和效果列表。
- `FaShuData`、`GongFaData`、`EffectData`：法术、功法和效果配置。
- `ItemDetails` 各派生类、`InventoryBag`、`StorageBagDetails`：物品、背包和装备配置。
- `SceneData_SO` 及世界层级数据：场景引用、网格与角色生成配置。

运行时角色会克隆 CharacterData、InventoryBag、ItemDetails 和 FaShuData，当前状态仅存在内存中。境界表使用 TextAsset/CSV 读取。项目没有发现 `PlayerPrefs`、JSON、二进制文件、数据库、云存档或统一 SaveData DTO；多处 TODO 也明确说明存档尚待实现。

建议下一阶段采用“配置 ID + 纯数据存档”的方案：

- ScriptableObject 只作为只读配置源，每种角色、物品、法术、功法、效果和场景均使用稳定唯一 ID。
- 存档使用不引用 UnityEngine.Object 的 DTO，至少拆分为 SaveHeader、WorldState、PlayerState、InventoryState、QuestState 和 SceneState。
- 保存角色基础数值、境界/经验、已学功法法术 ID 与运行时进度、背包物品 ID/数量/耐久或冷却、时间、场景 ID 和位置。
- 存档文件带 schemaVersion、游戏版本、时间戳和校验；使用临时文件写入后原子替换，并保留最近一次备份。
- 先实现单槽手动存读和场景切换自动存档，再扩展多槽与云同步。
- 不应直接序列化当前 ScriptableObject 克隆或依赖 Unity 实例 ID/GUID 作为长期存档格式。

## 八、当前已知 Bug 和技术债

高优先级问题：

- `EffectData.currentValue` 在 `value == 0` 时递归访问自身，会导致栈溢出；任何数值为 0 的效果都可能触发。
- `TimeManager` 的月份/季节推进逻辑存在边界错误：季节切换后直接把月份设为 4、7、10 等值，后续会每月再次切季，并可能产生枚举范围外的季节值。
- `SceneNameDrawer` 直接引用 `UnityEditor` 与 Addressables Editor API，却位于运行时默认程序集且没有 `#if UNITY_EDITOR` 或 Editor 专用 asmdef；Player Build 存在编译失败风险。
- `Player.Move` 先计算对角线归一化速度，随后无条件再次赋值原始速度，导致对角线移动更快。
- NPC 随机法术使用 `Random.Range(0, count - 1)`；整数上界不包含，最后一个法术永远不会被选中。
- `CharacterBase.UpdateEffectList` 遇到第一个非 Sustainable 效果会 `return`，导致后续效果不结算；移除一个到期效果后也会立即 `break`。
- 灵根总和为 0 时，道藏分配会先发生除零，并最多进行 9999 次无效循环。
- 战斗流程缺少统一状态机和结束清理，`isCombating`、网格障碍、角色字典、UI、时间暂停等状态可能残留。

结构性技术债：

- 多数系统依赖公共字段、全局单例和静态事件；初始化顺序、场景引用与事件退订错误容易造成空引用或隐式状态。
- `CharacterManager` 刚经历脚本重命名和场景 GUID 修复；其执行顺序属性已恢复，但当前工作区仍有未提交的 Unity 升级、场景和项目设置变更，需要整理成可审查提交。
- Addressable 场景通过 `AssetReference.LoadSceneAsync` 加载，却使用 `SceneManager.UnloadSceneAsync` 卸载，没有保存/释放 Addressables handle，可能造成引用计数和资源生命周期问题。
- 多个 `async void` 场景流程缺少取消、重入保护和异常处理；玩家连续触发传送可能造成并发装卸。
- A* 每次寻路对 open list 全量排序，且对已在 open list 的节点没有正确比较/更新更优 gCost；正确性和扩展性均需验证。
- InventoryManager 超过千行，包含大量相似分支和索引换算，维护与回归风险高。
- 默认程序集包含全部游戏代码，没有 runtime/editor/test 模块隔离；编译时间和依赖控制会随内容增长恶化。
- 配置字段命名混用中文、英文、PascalCase/camelCase，多个枚举与属性命名不一致，例如 Eloquence/Persuasion，后续数据迁移风险较高。
- 大量运行时数据仍为可变 ScriptableObject，效果对象还持有 from/target 等施法上下文；并发施法或共享效果实例时可能互相覆盖状态。
- 项目没有正式自动化测试。`AStarTest` 是场景脚本，不构成持续回归测试。
- 仓库包含 `.DS_Store`、`.idea` 等未跟踪文件，忽略规则和提交卫生需要完善。
- 字体目录包含标注“商用需授权”的字体，发行前必须确认许可或替换。

## 九、性能风险

- 战斗行动进度在 `FixedUpdate` 中遍历全部角色，并对字典执行 LINQ、`Values.Max()` 和筛选；单位数量增大时会产生额外 CPU 与 GC 压力。
- 网格范围查询采用矩形双重循环，目标检测又对路径列表执行 `Contains`；大地图、大范围法术和多单位组合时复杂度会迅速上升。
- A* 使用 `List.Sort()` 作为 open set，每次扩展都排序；应改为优先队列并减少节点重复加入。
- 道藏按“每一点资源一次随机抽样”的方式分配，资源上限高时会线性增加随机调用，异常数据还会跑到 9999 次。
- 背包和战斗 UI 每次刷新大量 Destroy/Instantiate 子对象；战斗行动槽字典也没有在新战斗前完全清理。建议对 Slot、Tooltip、行动条统一池化或增量更新。
- `FindAnyObjectByType`、`GameObject.FindWithTag` 和频繁 `GetComponent` 目前数量不大，但都集中在场景/战斗关键路径，应逐步改成显式注入或缓存。
- 角色属性与物品初始化会深度 Instantiate 多层 ScriptableObject；大规模 NPC 生成时会带来内存和加载尖峰。
- Addressables 场景 handle 未管理，长时间跨场景游玩可能积累未释放资源。
- Persistent 场景承载多个全局管理器和 UI 单例，若重复加载或初始化失败，当前 Singleton 实现只 Destroy 重复 GameObject，派生 Awake 仍可能继续执行部分逻辑。
- 项目包含完整 TMP Examples、多个第三方美术 Demo 和未使用包，可能增加导入时间、构建体积和资源检索噪音。

目前内容规模很小，性能问题大多不会立即显现；应在扩充地图、单位和物品前建立基准，而不是等内容完成后集中优化。

## 十、下一阶段开发计划

建议按以下顺序推进：

### 阶段 0：建立稳定基线（约 1 周）

- 整理当前 Unity 6000.4.8f1 升级与 CharacterManager 重命名变更，清理无关生成文件。
- 修复上述高优先级确定性 Bug。
- 将编辑器代码移入 Editor asmdef，建立 Runtime、Editor、Tests 基础程序集。
- 确认 Built-in 或 URP 的唯一渲染路线；若选 URP，完成项目级绑定和主要场景验证。
- 建立 macOS 开发构建和最小 CI，覆盖启动、场景加载及脚本编译。

### 阶段 1：完成一场可闭环战斗（约 2–3 周）

- 引入明确的 CombatState 状态机：准备、行动、结算、胜利、失败、退出。
- 完成死亡移除、胜负判定、奖励、时间恢复、UI/网格清理。
- 修复并测试 A*、移动占位、目标范围和连续施法。
- 将 Effect 改造成“无状态配置 + 独立运行时实例/上下文”，完成持续效果和回合生命周期。
- 至少加入 3–5 个有差异的法术、2–3 类敌人和基础战斗奖励，验证道藏选择是否真正产生策略。

### 阶段 2：建立可持续成长和存档（约 2–3 周）

- 确定稳定 ID 规范并实现版本化 SaveData。
- 支持时间、场景位置、角色成长、功法法术、背包装备的存读。
- 完成功法学习/装备、境界突破和法术熟练度的最小成长闭环。
- 把物品/背包业务拆成库存领域逻辑与 UI 展示，补齐核心单元测试。

### 阶段 3：建立垂直切片（约 4–6 周）

- 制作一个完整区域：探索、NPC、任务/事件、一次采集或交互、一场普通战斗和一场精英战斗。
- 加入奖励、商店或制作中的至少一条经济消耗路径。
- 完善菜单、设置、音频、引导和失败恢复。
- 用目标硬件对加载、战斗、UI 和内存进行 Profiling，确定内容预算。

完成垂直切片后再批量扩充功法、法术、装备、地图和剧情，避免在核心规则未验证前制造大量难以迁移的数据。

## 十一、需要重点讨论的设计问题

1. **产品主循环是什么？** 当前探索、战棋、修炼、装备、制作的结构都已预留，但主次尚不明确。需要确定玩家每 10 分钟、每 1 小时分别在做什么。
2. **战斗是传统轮流回合，还是行动条制？** 当前是实时累计行动条后进入单角色行动。需要明确等待期间是否暂停、速度属性如何影响行动频率，以及是否允许插队、打断和蓄力。
3. **五行道藏的核心决策是什么？** 当前随机按灵根分配且法术消耗部分固定、部分同色；需要决定玩家能否控制生成、储存、转化和跨回合保留，否则策略可能被随机性主导。
4. **境界差是否应强压命中与暴击？** 当前每个大境界约带来 50 个百分点修正，可能使低境界对高境界几乎完全无效。需要明确这是世界观硬门槛还是可通过构筑克服的软差距。
5. **功法、法术、法宝如何分工？** 三者都能提供属性或技能。建议明确：功法定义长期资源与规则，法术定义主动战术，法宝提供装备限制和局部规则改变。
6. **战斗规模与地图尺度是多少？** 这直接决定 A*、范围显示、行动条 UI、AI 和性能架构。当前实现更适合少量单位、小范围战斗。
7. **死亡与失败的代价是什么？** 需要确定角色永久死亡、读档、受伤、掉落或时间惩罚，才能设计存档时机与战斗结算。
8. **时间系统服务哪些玩法？** 当前时间非常快，并驱动年龄和修炼经验；需要定义战斗、移动、菜单、修炼操作分别消耗多少时间，以及寿元压力是否为核心机制。
9. **随机性边界在哪里？** 道藏生成、命中、暴击和 AI 都有随机性。需要给玩家提供预测、操控和补救手段，避免策略被连续随机判定淹没。
10. **数据生产流程如何建立？** 若法术、功法和物品数量较大，应尽早建立稳定 ID、校验器、批量编辑/导入工具和数据测试，而不是继续手工维护互相引用的 ScriptableObject。
11. **目标平台与渲染方案是什么？** 应尽快确定桌面端范围、最低硬件、分辨率和 Built-in/URP，避免美术、光照、Shader 与构建流程反复迁移。
12. **商业素材授权是否完整？** 部分字体明确标有商用授权提示，第三方像素素材和 GUI 包也需要在发行计划前完成许可证清单。

## 总体判断

项目已经搭建了一个覆盖面较广的修仙战棋原型，核心概念“五行道藏 + 功法/法术/法宝构筑”有形成独特玩法的潜力。当前最大风险不是缺少更多内容，而是基础状态管理、存档、战斗闭环和数据生命周期尚未稳定，同时系统铺设速度快于测试与架构收敛速度。

建议下一位顾问首先推动三项决策：确定战斗与道藏的核心规则、完成一场可重复且可结算的战斗闭环、建立可迁移的存档/数据 ID 基础。三项完成后，再评估内容量产与垂直切片计划。
